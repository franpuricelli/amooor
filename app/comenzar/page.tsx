import Wizard from "@/components/wizard/Wizard";

// El onboarding (WP-3). Vive en /comenzar; el CTA de marketing entra acá.
export const dynamic = "force-dynamic";

export default function ComenzarPage() {
  return <Wizard />;
}
